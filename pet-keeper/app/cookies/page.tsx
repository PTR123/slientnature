import CookiesContent from './page-content';

export const metadata = {
  title: 'Cookie 政策 - PetKeeper',
  description: 'PetKeeper Cookie 使用政策，了解我们如何使用本地存储技术',
};

export default function CookiesPage() {
  return <CookiesContent />;
}