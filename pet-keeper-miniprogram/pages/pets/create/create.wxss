/* pages/pets/create/create.wxss */
.container {
  padding: 20rpx;
  background-color: #fefdfb;
  min-height: 100vh;
  padding-bottom: 200rpx;
}

.form-card {
  padding: 30rpx;
}

.form-item {
  margin-bottom: 30rpx;
}

.form-label {
  display: block;
  font-size: 28rpx;
  font-weight: 500;
  color: #333;
  margin-bottom: 12rpx;
}

.required {
  color: #ff4444;
}

.form-input {
  width: 100%;
  height: 80rpx; /* 添加明确的高度 */
  padding: 0 24rpx; /* 调整padding，只保留左右 */
  line-height: 80rpx; /* 添加行高，使文字居中 */
  background-color: #f5f5f5;
  border-radius: 12rpx;
  font-size: 28rpx;
  box-sizing: border-box;
}

.form-picker {
  display: flex;
  justify-content: space-between;
  align-items: center;
  height: 80rpx; /* 与输入框高度一致 */
  padding: 0 24rpx; /* 左右padding */
  background-color: #f5f5f5;
  border-radius: 12rpx;
}

.picker-value {
  flex: 1; /* 占据剩余空间 */
  height: 80rpx; /* 与容器高度一致 */
  line-height: 80rpx; /* 文字在80rpx高度中居中，和输入框一样 */
  font-size: 28rpx;
  color: #333;
}

.picker-placeholder {
  flex: 1;
  height: 80rpx;
  line-height: 80rpx; /* 文字居中 */
  font-size: 28rpx;
  color: #999;
}

.picker-arrow {
  height: 80rpx; /* 与容器高度一致 */
  line-height: 80rpx; /* 箭头居中 */
  font-size: 28rpx;
  color: #999;
  padding-left: 12rpx; /* 与文字保持间距 */
}

.form-textarea {
  width: 100%;
  min-height: 150rpx;
  padding: 24rpx;
  background-color: #f5f5f5;
  border-radius: 12rpx;
  font-size: 28rpx;
  box-sizing: border-box;
}

/* 图片上传 */
.image-upload {
  width: 100%;
  min-height: 300rpx;
  background-color: #f5f5f5;
  border-radius: 12rpx;
  overflow: hidden;
  display: flex;
  align-items: center;
  justify-content: center;
}

.preview-image {
  width: 100%;
  height: 300rpx;
  object-fit: cover;
}

.upload-placeholder {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 60rpx 0;
}

.upload-icon {
  font-size: 80rpx;
  color: #ccc;
  line-height: 1;
  margin-bottom: 20rpx;
}

.upload-text {
  font-size: 28rpx;
  color: #999;
}

/* 按钮组 */
.button-group {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  padding: 20rpx 30rpx;
  background-color: #fefdfb;
  border-top: 2rpx solid #f0f0f0;
  display: flex;
  gap: 20rpx;
}

.button-group button {
  flex: 1;
  margin: 0;
}

.btn-primary {
  background-color: #4a784a;
  color: #fff;
  border-radius: 50rpx;
  padding: 24rpx;
  text-align: center;
  font-weight: 500;
  border: none;
}

.btn-primary:active {
  background-color: #3d633d;
}

.btn-secondary {
  background-color: #f5f5f5;
  color: #666;
  border-radius: 50rpx;
  padding: 24rpx;
  text-align: center;
  font-weight: 500;
  border: none;
}

.btn-secondary:active {
  background-color: #e8e8e8;
}